[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "idraw"
version = "1.0.0"
description = "idraw"
readme = "README.md"

requires-python = ">=3.10,<3.13"

dependencies = [
    "numpy<2",
    "imageio[ffmpeg]",
    "joblib",
    "matplotlib",
    "torch",
    "torchvision",
    "numba",
    "scikit_learn",
    "seaborn",
    "tqdm",
    "umap-learn",
]

authors = [
  { name = "Nolan Heffner", email = "nolan.heffner2@gmail.com" },
  { name = "Bryan Fichera", email = "bfichera@anl.gov" },
]
